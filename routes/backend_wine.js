const express = require('express');
const router = express.Router();

router.get('/wine', (request, response) => {

})

router.put('/inventory/list_name', (request, response) => {
    
})

module.exports = router;